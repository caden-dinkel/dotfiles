{
    disko.devices = {
        disk = {
            main = {
                type = "disk";
                device = "/dev/nvme0n1";
                content = {
                    
                };
            };

            secondary = {
                type = "disk";
                device = "/dev/sda";
            };
        };
    };
}