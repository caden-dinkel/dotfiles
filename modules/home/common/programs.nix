{ pkgs, ... }:
{
  programs.alacritty = {
    enable = true;
  };
  programs.obsidian = {
    enable = true;
  };
  programs.vscode = {
    enable = true;
  };
  programs.claude-code = {
    enable = true;
  };
  programs.ncspot = {
    enable = true;
  };
}