SECRETIVE
Store SSH keys in the Secure Enclave.
Hardware key backed ssh-agent for mac.

Hyprland/Wayland
managing desktop UI/tiling on linux personal machines.

Alacritty
Terminal that doesn't do tabs and stuff.