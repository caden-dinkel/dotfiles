This import should probably only happen once. I could also just include a conditional import. I may need to define a proper module for roles to make this work.

In the meantime, I can just ensure that this role is passed when dev, desktop, or personal are passed.